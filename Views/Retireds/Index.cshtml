@model IEnumerable<Comprehensive_backend.Models.Retired>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RetiredFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RetiredLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RetiredNationalCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RetiredFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RetiredLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RetiredNationalCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RetiredId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RetiredId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RetiredId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
